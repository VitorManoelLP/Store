spring:
  application:
    name: store
    profile:
      active: default
    cloud:
      config: http://localhost:8888/
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
    database: 0

server:
  port: 8081