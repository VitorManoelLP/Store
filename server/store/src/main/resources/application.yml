spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/store
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver
  application:
    name: store
    profile:
      active: dev
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
    database: 0

server:
  port: 8081